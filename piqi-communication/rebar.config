{cover_enabled, true}.
{deps, [
    {piqi, "", {git, "git://github.com/alavrik/piqi-erlang.git", {branch, "master"}}}
]}.

{pre_hooks, [
    {compile, "make -C server"}
]}.


{post_hooks, [
    {clean, "make -C server"}
]}.
